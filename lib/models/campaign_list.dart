class CampaignList {
  int id;
  String familyHeadName;
  double respondentName;
  String mobileNumber;
  String villageCode;
  String status;
  bool isSelected;

  CampaignList(this.id, this.familyHeadName, this.respondentName,
      this.mobileNumber, this.villageCode, this.status, this.isSelected);
}